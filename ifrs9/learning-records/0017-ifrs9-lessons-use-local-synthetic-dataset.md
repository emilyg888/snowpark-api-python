# IFRS9 lessons use the local synthetic dataset

The IFRS9 migration course should now use `/Users/emilygao/LocalDocuments/Projects/bb_datasets/ifrs9` as the hands-on source. Future lessons should prefer its SAS batch program, CSV schemas, SAS numeric date fields, lineage file, and expected ECL outputs over invented sample tables.
